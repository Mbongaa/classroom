# LiveKit Configuration
LIVEKIT_URL=wss://your-livekit-instance.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key

# OpenAI STT Model Configuration
# REQUIRED: This system exclusively uses GPT-4o transcribe model
# No fallback options - GPT-4o transcribe is mandatory for operation
#
# GPT-4o transcribe features:
# - Superior accuracy with lower Word Error Rate (WER)
# - Better multilingual support (100+ languages)
# - Improved handling of accents and noisy environments
# - Optimized for real-time streaming use cases
#
# IMPORTANT: Ensure your OpenAI API key has access to GPT-4o transcribe model
OPENAI_STT_MODEL=gpt-4o-transcribe

# Optional: Language Configuration
# Set to "auto" for automatic language detection or specify ISO 639-1 code (e.g., "en", "es", "fr")
# DEFAULT_TRANSCRIPTION_LANGUAGE=en